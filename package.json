{
  "name": "search-party",
  "version": "1.0.0",
  "description": "Collaborative web search tool - Google Docs for Googling",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "test": "cd client && npm test"
  },
  "keywords": [
    "search", 
    "collaboration", 
    "realtime", 
    "websocket", 
    "react", 
    "nodejs"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.1"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  }
}
